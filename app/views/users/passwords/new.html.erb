<section class="container">
  <div class="section columns">
    <div class="column is-6 is-offset-3 p-6">
      <h2 class="title has-text-centered has-text-info">Forgot your password?</h2>
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "box px-5" }) do |f| %>
        <%= render "users/shared/error_messages", resource: resource %>
        <div class="field">
          <%= f.label :email, class: "label" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "input" %>
        </div>
        <div class="field">
          <%= f.submit "Send me reset password instructions", class: "button is-info is-fullwidth is-medium mt-5" %>
        </div>
      <% end %>
      <%= render "users/shared/links" %>
    </div>
  </div>
</section>
